# ⭐ FocusWave — Aplicativo de Foco com IA, Pomodoro e Músicas Binaurais

O **FocusWave** é um aplicativo web que combina **inteligência artificial**, **músicas binaurais**, **Timer Pomodoro personalizável**, **pausa/retomada** e **histórico de músicas** para ajudar no foco, produtividade, estudo, relaxamento e sono.

---

## 🎯 Objetivo

Ajudar pessoas que lidam com dificuldade de foco, ansiedade, estresse e ambientes cheios de distrações, por meio de recomendações musicais inteligentes e ciclos de produtividade otimizados.

---

## 🚀 Funcionalidades do MVP

- Chat com **sugestões de músicas via IA**
- **Timer Pomodoro** com descanso
- **Pausar e retomar** o ciclo
- **Histórico** das músicas ouvidas
- **Login e cadastro** (opcional)
- Ambiente minimalista e focado

---

## 🏗️ Arquitetura do Projeto

```
root/
├── backend/       → API REST em Java puro (JAX-RS + Jersey + Grizzly)
├── frontend/      → HTML, CSS, JS
└── docs/          → Documentação geral
```

---

## ⚙️ Como Rodar o Projeto Localmente

### 1️⃣ Clonar o Repositório

```bash
git clone https://github.com/seu-usuario/focuswave.git
cd focuswave
```

---

# 🗄️ Configuração do Banco de Dados

## 2️⃣ Criar o arquivo `config.properties`

📍 Local:
```
backend/src/main/resources/config.properties
```

Conteúdo:
```
db.url=jdbc:oracle:thin:@localhost:1521:XE
db.user=SEU_USUARIO
db.pass=SUA_SENHA
```

---

# 📦 Executar as Tabelas (DDL + DML)

### 🔹 DDL — Criação das Tabelas
```sql
CREATE TABLE FW_User_Profile
(
    id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name       VARCHAR2(100) NOT NULL,
    email      VARCHAR2(150) NOT NULL,
    password   VARCHAR2(100) NOT NULL,
    CONSTRAINT UQ_EMAIL_PROFILE UNIQUE (email)
);

CREATE TABLE FW_Music
(
    id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    title       VARCHAR2(255) NOT NULL,
    description VARCHAR2(500),
    url         VARCHAR2(500) NOT NULL,
    category    VARCHAR2(50) NOT NULL
);

CREATE TABLE FW_MUSIC_HISTORY
(
    id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    id_user_profile NUMBER NOT NULL,
    id_music        NUMBER NOT NULL,
    played_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT FK_USER_MUSIC_HISTORY FOREIGN KEY (id_user_profile)
        REFERENCES FW_User_Profile (id) ON DELETE CASCADE,

    CONSTRAINT FK_MUSIC_HISTORY FOREIGN KEY (id_music)
        REFERENCES FW_Music (id) ON DELETE CASCADE
);
```

---

# ▶️ Rodar o Backend

```bash
cd backend
cd src
cd main
cd java
cd com.focuswave
java main
```

---

# 💻 Rodar o Frontend

```bash
cd frontend
npx live-server
```

ou abra `index.html` diretamente.

---

# 🔌 Endpoints Disponíveis

- `POST /chat/suggestions`
- `GET /history`
- `POST /auth/register`
- `POST /auth/login`
- `POST /auth/logout`

---

# 🧭 Roadmap do Projeto

- [x] Chat IA
- [x] Pomodoro
- [x] Pausar/Retomar
- [x] Trocar música
- [ ] Login/Cadastro
- [ ] Persistência completa
- [ ] UI final
- [ ] IA real usando GPT/Gemini

---

# 🤝 Como Contribuir

1. Fork
2. Branch
3. Commit
4. Pull Request

---

# 📝 Licença

MIT License
